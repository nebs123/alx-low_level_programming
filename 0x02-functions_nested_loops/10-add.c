#include "main.h"

/**
 * add - adds the inputs a and b then returns the result
 * @a: input to be added
 * @b: input to be added
 *
 * Return: returns sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
