#include "main.h"

/**
 * _abs - returns the absolute value of n
 * @n: the number whose abs value will be returned
 *
 * Return: returns n if n >=0 and returns -n if n<0
 */
int _abs(int n)
{
	return (n >= 0 ? n : -n);
}
