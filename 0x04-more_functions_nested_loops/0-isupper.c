#include "main.h"

/**
 * _isupper - checks if inputed character is uppercase
 * @c: decimal value of some character
 *
 * Return: returns 1 if c is upper case character
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
