#include "main.h"

/**
 * mul - returns the product of a and b
 * @a: 1st no to be multiplied
 * @b: 2nd no to be multpilied
 *
 * Return: returns an int which is a  product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
